{
  "name": "bootcamp-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build -w client && npm run build -w server",
    "deploy": "./scripts/deploy-cloud-run.zsh",
    "deploy:setup": "./scripts/setup-gcp-deploy.zsh",
    "deploy:verify": "./scripts/verify-cloud-run.zsh",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "seed": "npm run seed -w server",
    "set-coach": "npm run set-coach -w server",
    "typecheck": "npm run typecheck -w client && npm run typecheck -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
